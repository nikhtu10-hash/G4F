# Pin loosely — g4f ships frequent releases with provider fixes.
# If a run starts failing broadly after a g4f update, check their changelog
# first; internal attribute names (e.g. `needs_auth`, `models`) occasionally move.
g4f[all]>=0.4.0
